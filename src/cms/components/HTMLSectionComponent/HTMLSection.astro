---
import type { DisplaySettingsFragment, HtmlSectionFragment } from '../../../../__generated/sdk';
import type { ContentPayload } from '../../../graphql/shared/ContentPayload';
import { isEditContext } from '../../shared/utils.ts';

const isCmsEdit = isEditContext(Astro.url);

export interface Props {
    key: string;
    data: HtmlSectionFragment;
    displaySettings: DisplaySettingsFragment[];
    contentPayload: ContentPayload;
}
const { key, data, displaySettings} = Astro.props as Props;

const componentClass = 'component-paragraph';
console.log(data)
---

<div>
    <Fragment set:html={data.html} />

</div>