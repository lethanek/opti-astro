fragment HeroCarousel on HeroCarousel {
    Heros {
        _metadata {
            key
            types
            displayName
        }
        ... on Hero {
            ...Hero
        }
    }
}
